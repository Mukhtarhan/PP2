class Shape:
    class Square:
        def __init__(self,length):
            self.length=length
        def area(self):
            print(self.length*self.length)
    def area():
        print(0)
a=int(input())
s1=Shape.Square(a)
s1.area()
s2=Shape.area()
